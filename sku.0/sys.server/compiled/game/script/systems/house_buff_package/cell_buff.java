package script.systems.house_buff_package;/*
@Filename: script.systems.house_buff_package.
@Author: BubbaJoeX
@Purpose: Upon entry into a house cell, this script will check apply buffs based on the objvar.
*/

/*
 * Copyright © SWG:Resurgence 2023.
 *
 * Unauthorized usage, viewing or sharing of this file is prohibited.
 */

import script.library.player_structure;
import script.obj_id;
import script.location;
import script.menu_info_types;
import script.menu_info_data;
import script.dictionary;

public class cell_buff extends script.base_script
{
    /*public int OnAttach(obj_id self)
    {
        return SCRIPT_CONTINUE;
    }

    public int OnInitialize(obj_id self)
    {
        return SCRIPT_CONTINUE;
    }

    public int OnTransferred(obj_id self, obj_id sourceContainer, obj_id destContainer, obj_id transferer) throws InterruptedException
    {
        boolean top = player_structure.isBuilding(getTopMostContainer(destContainer))
        if (player_structure.isBuilding(getTopMostContainer(destContainer)))
        {
            if (hasObjVar(destContainer, "cell_buff"))
            {
                String buff = getStringObjVar(destContainer, "cell_buff");
            }
            return SCRIPT_CONTINUE;
        }
        else
        {
            return SCRIPT_OVERRIDE;
        }
        return SCRIPT_CONTINUE;
    }*/
}
